console.log(adding(2, 3));
//console.log(multiply(2, 3)); this returns an error!


function adding(x, y) {
	return (x + y);
}

var multiply = function(x, y) {
	return (x * y);
}

console.log(multiply(2, 3)); //this does not return an error